// El mismo 404 brutalist del root, pero dentro del layout del sitio:
// los notFound() de /nota, /jugador, /autor, etc. salen CON Nav y Footer.
// El de app/not-found.tsx queda para URLs fuera del route group.
export { default } from "../not-found";
